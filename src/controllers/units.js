export const getUnits = (req, res) => {
  res.json({
    message: "getting units",
  });
};

export const addUnits = (req, res) => {
  res.json({
    message: "adding units",
  });
};

export const editUnits = (req, res) => {
  res.json({
    message: "editing units",
  });
};

export const deleteUnits = (req, res) => {
  res.json({
    message: "deleting units",
  });
};
